#include <stdio.h>
/**
 * main - Entry point 
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x;

	c = 'a';
 
	for (x = 'A'; x <= 'Z'; x++)
		{
			char small = tolwer(x);

			putchar(small);
		}
	putchar('\n');
        return (0);
}

